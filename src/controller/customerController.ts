// customer.controller.ts
// customerController.ts

import { Request, Response } from "express"

// save customer
export const saveCustomer = (req: Request, res: Response) => {
  res.send("Ok")
}

// get all customers
export const getAllCustomer = (req: Request, res: Response) => {
  res.send("Ok")
}